package com.coinbase.android.signin;

import com.coinbase.android.ControllerScope;

@ControllerScope
public interface AcceptTermsControllerSubcomponent {
    void inject(AcceptTermsController acceptTermsController);
}
